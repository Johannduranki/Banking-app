import type { FaceVerificationProvider } from "./face-verification-provider.js";import type { BiometricCaptureReference } from "./biometric-types.js";import { mockResult } from "./mock-biometric-result.js";
export class MockFaceVerificationProvider implements FaceVerificationProvider{readonly providerName="MOCK_FACE";async createEnrollment(_customerId:string,_capture:BiometricCaptureReference){return mockResult("FACE_ENROLLMENT");}async verifyFace(_customerId:string,_enrollmentReference:string,_capture:BiometricCaptureReference){return mockResult("FACE_VERIFICATION");}async compareFaces(_customerId:string,_first:BiometricCaptureReference,_second:BiometricCaptureReference){return mockResult("FACE_COMPARISON");}}
